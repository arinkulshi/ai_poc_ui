# GCP Configuration
PROJECT_ID=your-project-id
DATA_STORE_ID=your-data-store-id
LOCATION=us

# Path to GCP service account key (absolute or relative to project root)
GOOGLE_APPLICATION_CREDENTIALS=credentials.json

# App Configuration (optional)
PORT=8080
FLASK_DEBUG=true
